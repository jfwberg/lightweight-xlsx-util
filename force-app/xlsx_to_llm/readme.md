# Lightweight - XLSX to LLM Util

A simple set of utilities to test the XLSX to LLM Text functions and see how it behaves in Agentforce




- Run the deployment using `sf project deploy start --source-dir force-app/xlsx_to_llm` (Or install the package)